apply plugin: 'com.android.application'

android {
    // ✅ GIỮ NGUYÊN 28: KHÔNG LỖI BUILD-TOOLS MỚI
    compileSdkVersion 28
    buildToolsVersion "28.0.3"

    defaultConfig {
        applicationId "com.example.thinkmobiles.bitcoinwalletsample"
        // ✅ 26 = ĐỦ JAVA 8 GỐC → KHÔNG CẦN DESUGAR
        minSdkVersion 26
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
        vectorDrawables.useSupportLibrary = true

        javaCompileOptions {
            annotationProcessorOptions {
                arguments = [
                    androidManifestFile: "$projectDir/src/main/AndroidManifest.xml".toString()
                ]
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    // ✅ JAVA 8 BÌNH THƯỜNG, KHÔNG CÓ DESUGAR GÂY LỖI
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

configurations.all {
    exclude group: 'com.google.code.findbugs', module: 'jsr305'
    exclude group: 'com.google.code.findbugs', module: 'annotations'
    exclude group: 'net.jcip', module: 'jcip-annotations'

    resolutionStrategy {
        // ✅ GUAVA 27.0.1-JRE: ĐÚNG BITCOINJ 0.17.1 CẦN
        force 'com.google.guava:guava:27.0.1-jre'
        force 'org.slf4j:slf4j-api:1.7.36'
        force 'com.google.protobuf:protobuf-java:3.11.4'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    // ✅ GIỮ NGUYÊN SUPPORT 28: KHÔNG ĐỔI ANDROIDX → KHÔNG SỬA CODE
    implementation 'com.android.support:appcompat-v7:28.0.0'
    implementation 'com.android.support:design:28.0.0'
    implementation 'com.android.support:support-v4:28.0.0'
    implementation 'com.android.support:recyclerview-v7:28.0.0'

    // ✅ ANDROID ANNOTATIONS 4.6.0 (CÓ TRÊN MAVENCENTRAL)
    annotationProcessor 'org.androidannotations:androidannotations:4.6.0'
    implementation 'org.androidannotations:androidannotations-api:4.6.0'

    // ✅ ZXING 3.5.0 (MAVENCENTRAL)
    implementation('com.journeyapps:zxing-android-embedded:3.6.0') {
        exclude group: 'com.google.code.findbugs'
        exclude group: 'net.jcip'
        exclude group: 'com.google.guava'
    }
    implementation 'com.google.zxing:core:3.4.1'

    // ✅ QRGEN (JITPACK → KHÔNG CẦN JCENTER)
    implementation 'com.github.kenglxn.QRGen:android:2.6.0'

    // ✅ BITCOINJ 0.17.1 (MAVENCENTRAL RÕ RÀNG)
    implementation('org.bitcoinj:bitcoinj-core:0.17.1') {
        exclude group: 'com.google.code.findbugs'
        exclude group: 'net.jcip'
        exclude group: 'com.google.guava'
        exclude group: 'org.slf4j'
        exclude group: 'com.google.protobuf'
    }

    // ✅ GUAVA CHÍNH THỨC
    implementation 'com.google.guava:guava:27.0.1-jre'

    // ✅ SLF4J ANDROID: KHÔNG CRASH THIẾU LOGGER
    implementation 'org.slf4j:slf4j-android:1.7.36'
}
