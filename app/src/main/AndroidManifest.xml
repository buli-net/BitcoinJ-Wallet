<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thinkmobiles.bitcoinwalletsample">

    <!-- ✅ CẦN CHO BITCOINJ KIỂM TRA KẾT NỐI MẠNG -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Quyền lưu file ví / blockchain cache -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        <!-- ✅ BẮT BUỘC: TẮT BACKUP → KHÔNG ĐƯA VÍ LÊN GOOGLE DRIVE (BẢO MẬT) -->
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        <!-- ✅ CHO PHÉP KẾT NỐI NODE BITCOIN KHÔNG TLS -->
        android:usesCleartextTraffic="true">

        <!-- ✅ THÊM exported="true" → BẮT BUỘC ANDROID 12+ MỞ APP ĐƯỢC -->
        <activity
            android:name=".main.MainActivity_"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
