name: Build BitcoinJ Wallet APK

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
  SKIP_JDK_VERSION_CHECK: "true"
  ANDROID_HOME: /usr/local/lib/android/sdk
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    name: Build MAINNET APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'zulu'
          cache: 'gradle'

      - name: Cấp quyền & Cài SDK đúng đường dẫn
        run: |
          chmod +x gradlew
          SDM="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDM" "build-tools;26.0.3" "platforms;android-26"

      - name: Build APK
        run: ./gradlew assembleDebug --stacktrace --no-daemon

      - name: Đổi tên APK
        run: |
          APK=$(find . -name "*debug*.apk" -type f | head -1)
          mv "$APK" BitcoinJ-Wallet-MAINNET.apk

      - name: Tải APK về
        uses: actions/upload-artifact@v4
        with:
          name: BitcoinJ-Wallet-APK
          path: BitcoinJ-Wallet-MAINNET.apk
